{
  "name": "backend",
  "version": "1.0.0",
  "description": "Node.js backend with in-memory database",
  "main": "src/index.js",
  "scripts": {
    "start": "nodemon src/index.js",
    "test": "jest --detectOpenHandles",
    "test:watch": "jest --detectOpenHandles --watchAll",
    "test:coverage": "jest --detectOpenHandles --coverage",
    "test:ci": "jest --detectOpenHandles --coverage --reporters=default --reporters=jest-junit"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "jest-sonar-reporter": "^2.0.0",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  }
}
